# Hostinger has no Python/C++ toolchain, so any native compile fails.
# better-sqlite3 ships prebuilt NAPI binaries in its tarball, so no build is ever needed.
# These flags also silence the pointless "node-gyp rebuild" npm synthesizes for it.
ignore-scripts=true
fund=false
audit=false